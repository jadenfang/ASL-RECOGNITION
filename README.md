# ASL-RECOGNITION

training dataset is dataset.zip (download the zip and upload through the first code box in colab)

when uploading the training or testing datasets make sure to upload zip files, the code will automatically unzip and organize them

can test the model on both labeled and unlabeled datasets (will just give predictions for unlabeled, will print accuracy for labeled)

can save and upload the model as keras files (we attached the best performing model we had as asl_detector.keras)

if you want to test the secret dataset used in the competition, upload it and test as a labeled dataset)
